package com.clinic.repositories;

import com.clinic.entities.AccompanimentScript;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccompanimentScriptRepository extends JpaRepository<AccompanimentScript, Long> {
}
